camera =
{
    resolution = { width = 800; height = 600; };
    position = { x = 0.0; y = 1.0; z = 2.5; };
    lookAt = { x = 0.0; y = 0.0; z = -1.0; };
    fieldOfView = 60.0; # In degree, assuming a reasonable FOV
};

# Primitives in the scene
primitives =
{
    # List of spheres
    spheres = (
        { x = -0.9; y = 0.5; z = -1.3; r = 0.4; color = { r = 255; g = 64; b = 64; }; },
        { x = -0.1; y = -0.1; z = -1.0; r = 0.15; color = { r = 255; g = 64; b = 64; }; }
    );

    # List of cylinders
    cylinders = (
        {
            position = { x = 0.8; y = -0.3; z = -1.1; };
            direction = { x = 0; y = 1; z = 0; };
            radius = 0.3;
            height = 0.8;
            color = { r = 64; g = 255; b = 64; };
            rotation = { x = 0; y = 0; z = 30; };
        }
    );

    # List of cones
    cones = (
        {
            position = { x = -0.4; y = 0.3; z = 1.5; };
            direction = { x = 0; y = 1; z = 0; };
            radius = 0.4;
            height = 0.8;
            color = { r = 64; g = 200; b = 64; };
            rotation = { x = 15; y = 0; z = 0; };
        }
    );

    # List of planes
    planes = (
        {
            axis = "Y"; # Using axis instead of normal vector
            position = -0.7; # Using a single value for position on the axis
            color = { r = 64; g = 64; b = 64; };
        }
    );
};

# Light configuration
lights =
{
    ambient = 0.4; # Ambient light multiplier (previously was 0.15, 0.15, 0.18)
    diffuse = 0.2;  # Adding diffuse multiplier as seen in example

    # List of directional lights
    directional = (
        {
            direction = { x = 0.3; y = -1.0; z = -0.5; };
            color = { r = 255; g = 255; b = 255; };
            intensity = 1.0;
        },
        {
            direction = { x = -0.6; y = -0.4; z = -0.2; };
            color = { r = 0.0; g = 0.0; b = 153; }; # 0.5, 0.5, 0.6 scaled to 255
            intensity = 0.7;
        }
    );

    # List of point lights
    point = (
        {
            position = { x = 1.0; y = 1.0; z = 0.0; };
            color = { r = 204; g = 0; b = 0; }; # 0.8, 0.8, 1.0 scaled to 255
            attenuation = { constant = 1.0; linear = 0.09; quadratic = 0.032; };
            intensity = 1.0;
        }
    );
};